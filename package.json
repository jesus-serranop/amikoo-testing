{
  "name": "amikoo-testing",
  "version": "1.0.0",
  "description": "Playwright test suite for Amikoo QA",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "@muuktest/amikoo-reporter": "latest",
    "dotenv": "^16.0.0"
  }
}
